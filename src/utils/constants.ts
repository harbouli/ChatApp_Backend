export enum Routes {
  AUTH = 'auth',
  USERS = 'users',
  CONVERSATION = 'conversation',
}

export enum Services {
  AUTH = 'AUTH_SERVICE',
  USERS = 'USERS_SERVICE',
  CONVERSATION = 'CONVERSATION_SERVICE',
}
